<%= form_for(@user_taxi_call) do |f| %>
  <% if @user_taxi_call.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_taxi_call.errors.count, "error") %> prohibited this user_taxi_call from being saved:</h2>

      <ul>
      <% @user_taxi_call.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
